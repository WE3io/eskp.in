services:
  postgres:
    image: postgres:16-alpine
    container_name: platform-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: platform
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      POSTGRES_DB: platform
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U platform"]
      interval: 10s
      timeout: 5s
      retries: 5

  # The application container — placeholder until the Claude instance
  # builds the actual application. This will be replaced.
  # app:
  #   build: .
  #   container_name: platform-app
  #   restart: unless-stopped
  #   ports:
  #     - "127.0.0.1:3000:3000"
  #   environment:
  #     DATABASE_URL: postgresql://platform:${POSTGRES_PASSWORD:-changeme}@postgres:5432/platform
  #     NODE_ENV: production
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3

volumes:
  postgres-data:
